name: "CodeQL"

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "30 3 * * 1"

jobs:
  changes:
    name: Detect changed layers
    runs-on: ubuntu-latest
    outputs:
      server: ${{ steps.filter.outputs.server }}
      client: ${{ steps.filter.outputs.client }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            server:
              - 'server/**'
            client:
              - 'client/**'

  analyze-server:
    name: Analyze (java-kotlin)
    needs: changes
    if: needs.changes.outputs.server == 'true' || github.event_name == 'schedule'
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: github/codeql-action/init@v3
        with:
          languages: java-kotlin
          build-mode: manual

      # Autobuild's default task selection (`testClasses`) OOMs the Kotlin
      # daemon on the standard GitHub-hosted runner. Running the compile task
      # in-process (inside the Gradle JVM, sized via -Xmx) instead of
      # spawning a separate, under-provisioned Kotlin daemon avoids that.
      - name: Build server
        working-directory: server
        run: |
          ./gradlew clean testClasses --no-daemon \
            -Dorg.gradle.jvmargs="-Xmx3g -XX:MaxMetaspaceSize=1g" \
            -Dkotlin.compiler.execution.strategy=in-process

      - uses: github/codeql-action/analyze@v3
        with:
          category: "/language:java-kotlin"

  analyze-client:
    name: Analyze (javascript-typescript)
    needs: changes
    if: needs.changes.outputs.client == 'true' || github.event_name == 'schedule'
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: github/codeql-action/init@v3
        with:
          languages: javascript-typescript
          build-mode: none

      - uses: github/codeql-action/analyze@v3
        with:
          category: "/language:javascript-typescript"
