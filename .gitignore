# Local environment configuration (copy from .env.example)
.env

# Database backups produced by backup.sh / backup.ps1
backups/

# Claude local settings
.claude/*.local*

# SonarQube CLI scratch cache (leaked by a known sonarqube-cli bug, see
# https://community.sonarsource.com/t/sonarqube-cli-leaves-behind-empty-sonar-scratch-directories-in-project-folder/184368)
.sonar/
