# Shorter idle timeout so the Gradle daemon (and anything attached to it, like the
# Testcontainers Ryuk reaper) exits soon after a build finishes instead of lingering.
org.gradle.daemon.idletimeout=60000
