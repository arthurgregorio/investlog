spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

server:
  address: '::'
  forward-headers-strategy: framework

investlog:
  demo-mode:
    enabled: true

  security:
    totp:
      enabled: false
