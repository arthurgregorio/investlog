<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-5.0.xsd">

    <changeSet id="25-1000-1" author="${claude}">
        <comment>Create system.trusted_devices, backing the "trust this device" 30-day TOTP skip (issue #173)</comment>
        <sql>
            CREATE TABLE system.trusted_devices (
                id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
                external_id UUID NOT NULL DEFAULT gen_random_uuid() UNIQUE,
                user_id BIGINT NOT NULL REFERENCES system.users(id),
                token_hash TEXT NOT NULL UNIQUE,
                label TEXT NOT NULL,
                created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
                last_used_at TIMESTAMPTZ NOT NULL DEFAULT now(),
                expires_at TIMESTAMPTZ NOT NULL
            );
        </sql>
        <rollback>
            <sql>DROP TABLE system.trusted_devices;</sql>
        </rollback>
    </changeSet>

    <changeSet id="25-1000-2" author="${claude}">
        <sql>CREATE INDEX idx_trusted_devices_user_id ON system.trusted_devices (user_id);</sql>
        <rollback>
            <sql>DROP INDEX system.idx_trusted_devices_user_id;</sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
